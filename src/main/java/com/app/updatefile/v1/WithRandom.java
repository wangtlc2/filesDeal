package com.app.updatefile.v1;

import java.io.RandomAccessFile;

import org.apache.log4j.Logger;

public class WithRandom {
	static Logger logger=Logger.getLogger(WithRandom.class);
	
	public static void main(String[] args) {
//		RandomAccessFile raf = new RandomAccessFile("wenjian.txt","rw");
//		char ch[] = new char[3];
//		char to[] = { 's', 's', 'x' };
//		boolean get = false;
//		while (get) {
//			raf.readChars(ch);
//			if (ch[0] == 'd' && ch[1] == 'd' && ch[2] == 's') {
//				get = true;
//			}
//		}
//		raf.seek(raf.getFilePointer() - 6);
//		raf.writeChars(to);
	}
}
